## HOMOLOG
## PROD

# =============================================================================
# CONFIGURAÇÕES DE AMBIENTE
# =============================================================================

# Node Environment (development | production | test)
NODE_ENV=development

# Porta da aplicação
PORT_NUMBER=3000

# =============================================================================
# CONFIGURAÇÕES DE BANCO DE DADOS
# =============================================================================

DATABASE_NAME=database-name
DATABASE_USER=database-user
DATABASE_PASSWORD=database-password-min-8-chars
DATABASE_HOST=localhost
DATABASE_PORT=5432

# =============================================================================
# CONFIGURAÇÕES DE SEGURANÇA - JWT
# =============================================================================

# IMPORTANTE: JWT_SECRET deve ter no mínimo 32 caracteres
# Em produção, use um valor forte e único gerado por ferramenta de secrets
# Exemplo de geração: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-required-for-production
JWT_EXPIRES_IN=7d
JWT_ISSUER=financeiro-api

# =============================================================================
# CRIPTOGRAFIA DE DADOS SENSÍVEIS
# =============================================================================

# ENCRYPTION_KEY - Chave AES-256 para criptografia de dados bancários e financeiros
# OBRIGATÓRIA: Sem essa chave, a aplicação NÃO INICIA
# Formato: 64 caracteres hexadecimais (32 bytes)
# Geração: openssl rand -hex 32
# IMPORTANTE: NUNCA compartilhe esta chave! Mantenha em local seguro
# Em produção: Use Secrets Manager (AWS, Azure, Google Cloud, Vault)
ENCRYPTION_KEY=your-encryption-key-64-hex-chars-use-openssl-rand-hex-32

# =============================================================================
# CONFIGURAÇÕES HTTPS/SSL
# =============================================================================

# Habilitar HTTPS (true | false)
# Em desenvolvimento: false (use HTTP) ou true (com certificados self-signed)
# Em produção: true (com certificados válidos de uma CA)
ENABLE_HTTPS=false

# Caminhos para certificados SSL (obrigatório se ENABLE_HTTPS=true)
# Para desenvolvimento, gere certificados com: npm run generate:ssl
SSL_KEY_PATH=./ssl/server.key
SSL_CERT_PATH=./ssl/server.cert

# =============================================================================
# CONFIGURAÇÕES DE CORS
# =============================================================================

# Origem permitida para requisições CORS
# Em desenvolvimento: http://localhost:3001
# Em produção: https://seu-dominio.com (use HTTPS!)
CORS_ORIGIN=http://localhost:3001
CORS_CREDENTIALS=true

# =============================================================================
# SECRETS MANAGER (Opcional - para Produção)
# =============================================================================

# Use serviços de gerenciamento de secrets em produção:
# - AWS Secrets Manager
# - HashiCorp Vault
# - Azure Key Vault
# - Google Secret Manager

USE_SECRETS_MANAGER=false
AWS_REGION=us-east-1
SECRETS_MANAGER_SECRET_NAME=financeiro-api/prod

# =============================================================================
# INSTRUÇÕES DE SEGURANÇA
# =============================================================================

# ⚠️  NUNCA commite o arquivo .env no Git!
# ✅ Use .env apenas para desenvolvimento local
# ✅ Em produção, use variáveis de ambiente do sistema ou serviços de secrets
# ✅ Gere JWT_SECRET forte: openssl rand -base64 32
# ✅ Use senhas de banco fortes (mínimo 12 caracteres, letras, números, símbolos)
# ✅ Habilite HTTPS em produção com certificados válidos
# ✅ Rotacione secrets regularmente em produção